# C++ Waveform Generator

# Installation and Instruction:

- Clone the repo.  
    ```bash
    git clone https://github.com/Akhilesh29/Cpp-Waveform-Generator.git 

- install Matplotlib using 'pip install matplotlib'.

- Compile the code with the 'matplotlib-cpp' library and run it to see the waveforms plotted.